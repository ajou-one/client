import S from "./Header.module.css";

const Header = () => {
    return (
        <header className={S["main-header"]}>
            <span className={S["main-header-title"]}>AJOU ONE</span>
        </header>
    )
};

export default Header;
